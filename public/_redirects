# Cache static assets forever
/_next/static/* /=200! Cache-Control: public,max-age=31536000,immutable

# Serve fresh HTML on every request
/index.html /=200! Cache-Control: public,max-age=0,must-revalidate
/ /index.html 200

# Catch-all: serve index.html for client-side routing
/* /index.html 200